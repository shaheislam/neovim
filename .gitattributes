# =============================================================================
# Git Attributes for Neovim Config (LazyVim)
# =============================================================================
# Merge/diff drivers registered globally via ~/dotfiles/scripts/setup.sh.

# -----------------------------------------------------------------------------
# Merge Drivers
# -----------------------------------------------------------------------------
# Keep-ours for lockfile - regenerated by Lazy after merge
lazy-lock.json merge=lockfile

# Union merge for docs modified across worktrees
CLAUDE.md merge=union-doc
AGENTS.md merge=union-doc
**/AGENTS.md merge=union-doc
TODO.md   merge=union-doc

# -----------------------------------------------------------------------------
# Line Endings
# -----------------------------------------------------------------------------
*.lua  text eol=lf
*.md   text eol=lf
*.json text eol=lf
*.toml text eol=lf
*.yml  text eol=lf

# -----------------------------------------------------------------------------
# Diff Drivers
# -----------------------------------------------------------------------------
# Sorted-key JSON diffs for lockfile and mise config
*.json diff=json

# -----------------------------------------------------------------------------
# Linguist
# -----------------------------------------------------------------------------
*.lua linguist-language=Lua
