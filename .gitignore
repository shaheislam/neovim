# Checkpoint pending data (ephemeral)
.checkpoints/

# Neovim debug log
.nvimlog

# Repo-local annotations and temporary agent scratch files
.tmp/

# Rust build output (rust-analyzer)
target/

# Override global gitignore — these are intentional project files
!CLAUDE.md
!AGENTS.md
!**/AGENTS.md
