---
ticket: "TASK"
title: "Apply nvim-best-practices fixes"
created: "2026-04-26T00:00:00Z"
last_updated: "2026-04-26T00:00:00Z"
---

# Plan: TASK — nvim-best-practices follow-up

## Objective

Apply the highest-value fixes from the `lumen-oss/nvim-best-practices` audit without overfitting plugin-author guidance to a personal config.

## Approach

1. Refactor custom kubectl helpers into a normal config module with one scoped command
2. Add a custom health module for runtime-only integrations
3. Add config validation and LuaCATS annotations to larger custom modules
4. Add CI for startup, health, and existing tests
5. Update stale repo documentation

## Progress

- [ ] Refactor kubectl helper wiring
- [ ] Add `nvim_mini` health check
- [ ] Add config validation and type annotations
- [ ] Add CI workflow
- [ ] Refresh README

## Key Decisions

- Keep fixes focused on custom in-repo modules rather than third-party plugin code.
- Treat plugin-author-only advice as optional unless it improves this personal config directly.
- Preserve existing `<leader>k` UX while reducing command namespace sprawl.

## Success Criteria

- `kubectl` helpers load from a normal config module and expose one scoped command
- `:checkhealth nvim_mini` reports on the custom runtime integrations
- Custom runtime modules validate config and expose basic type annotations
- CI covers startup, health, and the existing headless test
- README reflects the current repo shape

## Current State

Audit complete. Highest-value gaps were `lua/plugins/kubectl.lua`, missing health checks, sparse LuaCATS coverage on custom modules, missing config validation, stale README content, and no CI beyond Docker image publishing.

## Failed Approaches

(none yet)

## Next Steps

1. Validate headlessly
2. Fix any startup/test regressions
3. Commit if requested

## Useful Commands

```bash
nvim --headless +qa
nvim --headless "+checkhealth nvim_mini" +qa
nvim --headless -l tests/parley_review_spec.lua
```
