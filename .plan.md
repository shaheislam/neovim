---
ticket: "TASK"
title: "upgrade + skill"
created: "2026-03-29T18:08:49Z"
last_updated: "2026-03-29T21:03:00Z"
---

# Plan: TASK

## Objective

Complete the Neovim upgrade work and add project-level Claude/Codex guidance and a reusable Neovim skill.
# neovim — Task Prompt  Describe your task here.

## Approach

1. Scan for deprecated Neovim APIs (`vim.loop`, `vim.validate`, etc.)
2. Upgrade Neovim via Homebrew and refresh plugins via Lazy
3. Fix any compatibility regressions found during startup and editing flows
4. Add project documentation in `CLAUDE.md`
5. Add `.claude/skills/neovim/SKILL.md` for plugin, keymap, LSP, and debugging workflows
6. Verify clean startup and basic workflow health

## Progress

- [x] Investigation / codebase understanding
- [x] Implementation (upgrade, compatibility fixes, `CLAUDE.md`, skill)
- [x] Testing (headless startup, deprecated API grep)
- [x] Verification (clean load, zero `vim.loop`, merge conflict resolved)

## Key Decisions

- Replaced `vim.loop` with `vim.uv` in config code to match current Neovim APIs without changing behavior
- Added a `vim.bo.modifiable` guard to the `BufWritePre` autocmd to avoid E21 on read-only buffers
- Upgraded Neovim from `0.11.5` to `0.11.7` and updated plugins alongside it
- Added project-level agent guidance in `CLAUDE.md` plus a repo-local Neovim skill for future work
- Kept a single merged `.plan.md` record instead of discarding either branch's session notes

## Success Criteria

- Zero `vim.loop` references in tracked config code ✅
- Neovim upgraded to the intended stable version ✅ (`0.11.7`)
- Plugins updated successfully ✅
- `CLAUDE.md` and `.claude/skills/neovim/SKILL.md` added ✅
- Config loads cleanly and merge state is resolved ✅

## Current State

Upgrade and documentation work are merged together cleanly. A post-merge LSP keymap regression was identified and fixed by using the correct `buffer` option for `vim.keymap.set`.

## Useful Commands

_Save commands here that produced valuable results or solved problems.
Include what the command does and why it matters. Format:_

```bash
# Description of what this does and why it's useful
command --with-flags
```
